# Legacy adapter name, kept so existing .codecannon.yaml files keep working.
# Codex CLI reads the shared .agents/skills/ directory (not .codex/skills/),
# so this resolves to the `agents` adapter.
alias: agents
