# Legacy adapter name, kept so existing .codecannon.yaml files keep working.
# Cursor reads Agent Skills from both .agents/skills/ and .cursor/skills/;
# the old .cursor/rules/*.mdc output is superseded. Resolves to `agents`.
alias: agents
