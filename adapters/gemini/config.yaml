# Legacy adapter name, kept so existing .codecannon.yaml files keep working.
# Gemini CLI reads the shared .agents/skills/ alias with higher precedence
# than .gemini/skills/, so this resolves to the `agents` adapter.
alias: agents
