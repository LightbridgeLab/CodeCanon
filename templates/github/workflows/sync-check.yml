name: Sync check
on: [push, pull_request]
jobs:
  sync-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for sync drift
        run: |
          CodeCanon/sync.sh --dry-run
          # exits non-zero if any files would be written
